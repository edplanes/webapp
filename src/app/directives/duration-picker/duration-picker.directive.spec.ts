import { DurationPickerDirective } from './duration-picker.directive';

describe('DurationPickerDirective', () => {
  it('should create an instance', () => {
    const directive = new DurationPickerDirective();
    expect(directive).toBeTruthy();
  });
});
